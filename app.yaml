# How to deploy:
# gcloud app deploy app.yaml


service: dashboardfrontend
runtime: nodejs16

# Force https and redirect http to https
handlers:
- url: /.*
  secure: always
  redirect_http_response_code: 301
  script: auto

env_variables:
  NEXT_PUBLIC_PROD_DOMAIN: ".freelancedatascientist.net"